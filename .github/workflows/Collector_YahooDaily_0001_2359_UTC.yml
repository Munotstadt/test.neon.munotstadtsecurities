name: Yahoo Collector (Collector_YahooDaily_0001-2359_UTC)

on:
  schedule:
    - cron: '18,48 * * * *'
  workflow_dispatch: {}

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install yfinance psycopg2-binary

      - name: Run collector
        run: python scripts/collect_yahoo_daily_0001_2359_utc.py
        env:
          NEON_DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
